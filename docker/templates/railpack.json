{
  "deploy": {
    "base": {
      "image": "debian:bookworm-slim"
    }
  },
  "phases": {
    "install": {
      "cmds": [
        "npm install --prefer-offline --no-audit --no-fund --no-progress --legacy-peer-deps || yarn install --prefer-offline --no-progress --no-immutable || pnpm install --prefer-offline --no-audit --no-progress --no-frozen-lockfile || bun install --prefer-offline --no-audit"
      ]
    },
    "build": {
      "cmds": [
        "npm run build --if-present || yarn build || pnpm run build || bun run build || true",
        "npm prune --omit=dev || yarn workspaces focus --production || pnpm prune --prod || bun pm untrust || true",
        "rm -rf /root/.npm /root/.yarn /root/.cache /root/.bun /tmp/* /var/cache/apt/* /var/lib/apt/lists/* || true"
      ]
    }
  },
  "variables": {
    "NODE_ENV": "production",
    "CI": "true",
    "DO_NOT_TRACK": "1",
    "NPM_CONFIG_LEGACY_PEER_DEPS": "true",
    "BUN_INSTALL_FROZEN_LOCKFILE": "false",
    "COMPOSER_ALLOW_SUPERUSER": "1"
  }
}
